meta {
  name: Place Order (for payment tests)
  type: http
  seq: 1
}

post {
  url: {{orderingUrl}}/api/orders
  body: json
  auth: none
}

body:json {
  {
    "customer": {
      "email": "payment@example.com",
      "fullName": "Payment Test",
      "street": "Paystraat 1",
      "city": "Rotterdam",
      "postalCode": "3011AB",
      "country": "NL"
    },
    "items": [
      { "productId": "{{productId}}", "quantity": 1, "price": 0 }
    ]
  }
}

script:post-response {
  const data = res.getBody();
  if (data && data.id) {
    bru.setVar("orderId", data.id);
    bru.setEnvVar("orderId", data.id);
  }
}
