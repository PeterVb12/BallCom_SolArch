meta {
  name: Wait For Shipment (logistics async)
  type: http
  seq: 7
}

get {
  url: {{logisticsUrl}}/api/shipments/order/{{servicePortalOrderId}}
  body: none
  auth: none
}

script:pre-request {
  await new Promise((resolve) => setTimeout(resolve, 2000));
}

tests {
  test("shipment exists for service portal order", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
