# BallCom_SolArch hoihoi

# To run Ordering microservice

dotnet run --urls="http://localhost:5100"

# To run BallCom customer portal (BallCom.API)

dotnet run --urls="http://localhost:5000"

# Run rabbitMQ overview, Login with guest guest

http://localhost:15672

# Run docker compose file

docker-compose up
